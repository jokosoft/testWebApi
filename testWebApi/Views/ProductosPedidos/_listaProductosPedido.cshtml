@model IEnumerable<ModeloPedidos.Clases.ProductosPedidos>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Productos.Nombre_prod)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Productos.Nombre_prod)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id_productosPedido }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id_productosPedido }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id_productosPedido })
        </td>
    </tr>
}

</table>
