@model IEnumerable<ModeloPedidos.Clases.Restaurantes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Restaurante)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Restaurante)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Restaurante }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Restaurante }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Restaurante })
        </td>
    </tr>
}

</table>
